# AУДИТ САЙТА FERES

## 1. ПРОБЛЕМЫ ТИПОГРАФИКИ И СЕМАНТИКИ HTML

### A. Заголовки (H1–H4)
Проблемы в `index.html`:
- ✗ H3 используется для основных секций вместо H2 (нарушение семантики)
  - `<h3 class="h3">` в "Две серии продукции"
  - `<h3 class="h3">` в "Развитие компании"  
  - `<h3 class="h3">` в "Почему FERES?"
  
- ✗ H2 с классом `.h3` (противоречие семантика ≠ стиль)
  - `<h2 class="h3 mt-16">` в распределении товара
  
- ✗ Inline `style="font-size:16px;font-weight:700"` на H3 в каталоге (17 элементов)
  - Нарушает типографическую систему
  - Дублирует стили `.cat-category-title`
  
- ✗ Inline `style="margin-top:60px;margin-bottom:30px"` на H2 в финальном CTA
  - Должны быть CSS классы

### B. Шрифты
- ✓ PF Din Text Cond Pro подключен корректно
- ⚠ Узкий condensed-шрифт может быть неудобен для длинного текста
  - Используется для всех заголовков и тела
  - На мобильных размеры становятся очень мелкими

### C. Размеры шрифтов (проверено в CSS)
```
--fs-h1: clamp(38px, 7.2vw, 84px)   — OK
--fs-h2: clamp(27px, 3.4vw, 46px)   — OK
--fs-h3: clamp(20px, 1.7vw, 26px)   — OK
--fs-h4: 18px                        — без clamp, фиксированный
--fs-body: 17px                      — фиксированный, узкий для мобильных
--fs-small: 15px                     — OK
```
⚠ H4 и body-текст не масштабируются — жестко 18px и 17px

## 2. ПРОБЛЕМЫ СТРУКТУРЫ И КОНТЕЙНЕРОВ

### Секции главной страницы:
1. Hero (ширина 100vh, полноширинная) ✓
2. Качество (переменная ширина, 50/50 сетка) ⚠
3. Серии продукции (переменная ширина) ⚠
4. Каталог (переменная ширина, категории) ⚠
5. Партнёры / дистрибьюторы / рознице (переменная) ⚠
6. Развитие компании (таймлайн + метрики) ⚠
7. Почему FERES? (4 карточки) ✓
8. Где приобрести (финальный CTA) ⚠

### Контейнеры:
- `.wrap`: max-width: 1440px, padding-inline: var(--gutter) ✓
- `--gutter`: clamp(18px, 4vw, 48px) ✓
- `--page`: 1440px ✓
- `--sec-y`: clamp(56px, 7vw, 116px) для вертикальных отступов ✓

**Проблемы контейнеров:**
- ✗ Отступ между секциями неконсистентен
  - Некоторые секции используют `--sec-y`, другие собственный padding
- ✗ На мобильной (360px): содержимое может быть узким (~300px)
  - Нужна минимальная ширина для читаемости

## 3. ПРОБЛЕМЫ ВЕРТИКАЛЬНОГО РИТМА

Текущее состояние:
```
--sec-y: clamp(56px, 7vw, 116px)  — основной отступ секций
margin-bottom: .5rem на заголовках в некоторых местах
```

**Проблемы:**
- ✗ Разные секции имеют разные отступы
- ✗ Расстояние между заголовком и контентом неупорядочено
- ✗ `.sec-head` имеет gap: 14px, но margin-bottom: clamp(26px, 3vw, 46px)
- ✗ Нет единой шкалы отступов (4, 8, 12, 16, 24, 32, 48, 64...)

## 4. ПРОБЛЕМЫ СПЕЦИФИЧЕСКИХ СЕКЦИЙ

### A. «Развитие компании» (таймлайн)
- ✗ Огромный gap от заголовка до таймлайна
- ✗ Неактивные годы (не выбранные элементы) не читаются
- ✗ Расстояние от таймлайна до описания неконсистентно
- ✗ На мобильной: 8 точек сжимаются

### B. «Почему FERES?»
- ✓ 4 карточки согласованы
- ⚠ Две длинные подписи кнопок могут переноситься
- ⚠ Вводный текст очень большой, может конкурировать с кнопками

### C. «Где приобрести продукцию FERES?»
- ✗ Заголовок с inline style: `margin-top:60px;margin-bottom:30px`
- ✗ Две группы (партнёры / рознице) не связаны визуально
- ✗ На мобильной: две кнопки стоят рядом, неудобно нажимать

### D. Футер (finale)
- ✗ Очень простой дизайн, не согласован с премиальностью
- ✗ Социальные иконки мелкие
- ✗ Не хватает визуальной иерархии

## 5. ПРОБЛЕМЫ КНОПОК

Текущие варианты:
```
.btn--primary:   песочный градиент, чёрный текст
.btn--ghost:     прозрачная, песочная граница
.btn--quiet:     тёмный фон, граница
.btn--sm:        --btn-py:14px, font-size:16px
.btn--lg:        --btn-py:19px, font-size:16px
```

**Проблемы:**
- ✗ .btn--sm и .btn--lg имеют одинаковый font-size (16px)
  - Различие только в padding
- ✗ Нет явного `.btn--secondary` варианта
- ✗ На скриншотах видны овальные кнопки (border-radius: 999px), но в CSS border-radius: var(--r) (2px)
  - `.pill` класс существует, но не используется единообразно

## 6. ПРОБЛЕМЫ КАРТОЧЕК

### .card (базовая)
- ✓ Согласована с grid
- ⚠ Трансформация на hover: translateY(-3px) может быть неощутима

### .tile (плитка "почему")
- ✓ height: 100%
- ✓ gap: 12px
- ⚠ Иконка 42px может быть маленькой на больших экранах

### .series-card (серии)
- ✓ Медиа-зона 300px
- ⚠ На мобильной: медиа занимает 300px высоты, контент очень сжат

## 7. АДАПТИВНОСТЬ

Breakpoints в CSS:
- 640px (hero height)
- 720px (cols-2/3, footer)
- 900px (series-grid, anti-fake)
- 1040px (cols-4, header__cta)
- 1100px (header__top, nav, burger)
- 1280px (header__phone)

**Проблемы:**
- ✗ Много breakpoints (не согласованы)
- ✗ На 360px-390px: тесно, возможны обрезания
- ✗ На 640-720px: переходная зона, непредсказуемые переносы

## 8. КОНТРАСТ И ЦВЕТА

- ✓ Палитра: чёрный + песочный + оттенки серого
- ⚠ Текст var(--text-2) на тёмном фоне может быть маловат для WCAG AA
- ⚠ На некоторых элементах используется var(--text-3) (очень светлый серый)

## 9. ИНТЕРАКТИВНОСТЬ

- ✓ Focus-visible правильно стилизован
- ⚠ Таймлайн: нет явного фокуса на кнопках временных точек
- ⚠ Нет визуального feedback при loading

## РЕЗЮМЕ ПРОБЛЕМ

| Категория | Кол-во | Критичность |
|-----------|--------|-------------|
| Семантика HTML | 6 | Высокая |
| Inline стили | 18+ | Высокая |
| Типографика | 5 | Средняя |
| Контейнеры | 3 | Средняя |
| Отступы | 4 | Средняя |
| Адаптивность | 3 | Средняя |
| Кнопки | 3 | Низкая |
| Карточки | 3 | Низкая |

---

## ПЛАН ИСПРАВЛЕНИЙ

### Фаза 1: Критичные исправления (семантика)
1. Заменить все H3 на H2 для основных секций
2. Удалить inline `style="font-size:..."` и использовать CSS классы
3. Исправить H2 class="h3" на правильное использование

### Фаза 2: Типографика и отступы
1. Добавить clamp для --fs-h4 и --fs-body
2. Упорядочить вертикальный ритм по единой шкале
3. Унифицировать margin-bottom для заголовков

### Фаза 3: Компоненты и секции
1. Обновить кнопки (добавить .btn--secondary)
2. Улучшить .tile иконки (адаптивный размер)
3. Улучшить финальный CTA раздел

### Фаза 4: Тестирование
1. Проверка на 360, 390, 768, 1024, 1440 px
2. Проверка контраста (WCAG AA)
3. Проверка фокуса на всех интерактивных элементах
